image: node:20

stages:
  - e2e

e2e-tests:
  stage: e2e

  before_script:
    - corepack enable
    - yarn install
    - npx playwright install --with-deps

  script:
    - yarn run e2e

  artifacts:
    when: always
    paths:
      - playwright-report/
      - test-results/
